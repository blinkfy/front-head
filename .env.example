# Optional API origin override.
#
# Leave empty for H5 development and for an H5 build served by newftx. The
# browser will use its current origin, including Vite's /api and /ws proxies.
# Set an absolute URL only when the frontend and backend use different origins.
VITE_API_BASE_URL=
